import { LucideIcon } from "lucide-react";

export type LayoutProps = {
	children: React.ReactNode;
	uploadQuestion: React.ReactNode;
}

export type TitleBlockProps = {
	text: string;
	Icon: LucideIcon;
}